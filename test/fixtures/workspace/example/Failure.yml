info:
  name: Failure
  type: http
  seq: 2
http:
  method: GET
  url: "{{baseUrl}}/failure"
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: 201
  scripts:
    - type: tests
      code: |
        test("returns the expected status", function () {
          expect(res.status).to.equal(201);
        });
