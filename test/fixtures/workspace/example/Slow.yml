info:
  name: Slow
  type: http
  seq: 3
http:
  method: GET
  url: "{{baseUrl}}/slow"
runtime:
  scripts:
    - type: tests
      code: |
        test("returns HTTP 200", function () {
          expect(res.status).to.equal(200);
        });
