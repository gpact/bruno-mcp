info:
  name: Health
  type: http
  seq: 1
http:
  method: GET
  url: "{{baseUrl}}/health"
runtime:
  scripts:
    - type: tests
      code: |
        test("returns HTTP 200", function () {
          expect(res.status).to.equal(200);
        });
