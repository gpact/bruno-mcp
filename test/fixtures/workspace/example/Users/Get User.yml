info:
  name: Get User
  type: http
  seq: 1
http:
  method: GET
  url: "{{baseUrl}}/auth-echo"
  headers:
    - name: Authorization
      value: "Bearer {{apiKey}}"
runtime:
  scripts:
    - type: tests
      code: |
        test("echoes the authorization header", function () {
          expect(res.status).to.equal(200);
        });
